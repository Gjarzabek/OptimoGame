export enum CHARACTER_NAME {
    VIKING = "Viking"
}

export enum CHARACTER_STATE {
    STANDING,
    RUN_LEFT,
    RUN_RIGHT
}

export enum MOVE_DIRECTION_X {
    LEFT = -1,
    IN_PLACE,
    RIGHT
}
